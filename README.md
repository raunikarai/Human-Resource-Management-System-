# Human-Resource-Management-System-
A production-ready, lightweight HRMS application featuring employee CRUD operations and daily attendance tracking. Tech stack: React (Vite) frontend, Python FastAPI backend, SQLAlchemy ORM with SQLite. Deployable to Vercel + Render.
